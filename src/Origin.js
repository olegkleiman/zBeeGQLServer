// @flow

class Origin {

    id: string
    originId: number
    name: string

    constructor(id: string, originId: number, name: string) {
      this.id = id;
      this.originId = originId;
      this.name = name;
    }
}

export default Origin;
